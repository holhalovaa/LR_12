# PROMPT_LOG — Лабораторная работа №12

**Студент:** Холхалова Алина Сергеевна  
**Группа:** 220032-11  
**Вариант:** 28 (Система управления автосервисом)  
**Уровень:** Средняя сложность (задания 1–10)  
**Тема:** AI-ассистированная разработка (vibe coding)

---

## Задание 1: Генерация CRUD-приложения

### Промпт 1 (первый подход)
**Инструмент:** DeepSeek  
**Промпт:** 
> Ты — senior Python разработчик. Напиши FastAPI приложение для управления заказами в автосервисе. Модель заказа (Order): id (int), car_number (str), owner_name (str), description (str), status (str: "принят", "в работе", "готов", "выдан"), created_at (datetime), total_price (float). Сделай CRUD эндпоинты: GET /orders, POST /orders, GET /orders/{id}, PUT /orders/{id}, DELETE /orders/{id}. Используй Pydantic для валидации, добавь обработку ошибок 404 и 422. Храни данные в списке в памяти (list). Добавь type hints и документацию. Выдай полный код.

**Ответ ИИ:**
ИИ выдал полный код `main.py` с использованием FastAPI, Pydantic, списка в памяти. В коде были все эндпоинты, валидация через Field, обработка ошибок 404. Также был добавлен блок `if __name__ == "__main__": uvicorn.run(app, host="0.0.0.0", port=8000, reload=True)` для запуска.

**Мои действия:**
Я скопировала код, установила зависимости `pip install fastapi uvicorn pydantic` и попыталась запустить.

**Что пошло не так:**
При запуске `python main.py` получила ошибку: `SystemExit: 1. WARNING: You must pass the application as an import string to enable 'reload' or 'workers'`.

---

### Промпт 2 (исправление ошибки запуска)
**Инструмент:** DeepSeek  
**Промпт:** 
> Ошибка при запуске: Exception has occurred: SystemExit 1. "You must pass the application as an import string to enable 'reload' or 'workers'". Как исправить? Я использую Windows, Python 3.13.

**Ответ ИИ:**
ИИ объяснил, что проблема в параметре `reload=True` при прямом запуске файла. Предложил два решения:
1. Убрать `reload=True` из `uvicorn.run()`
2. Или удалить блок `if __name__ == "__main__"` и запускать через `uvicorn main:app --reload`

Также объяснил, что второй способ является профессиональным стандартом, так как позволяет перезагружать сервер при изменениях кода.

**Мои действия:**
Я выбрала второй способ, так как он лучше для разработки. Удалила блок `if __name__ == "__main__"` из `main.py` и строку `import uvicorn`, так как она больше не нужна.

---

### Промпт 3 (уточнение про запуск)
**Инструмент:** DeepSeek  
**Промпт:** 
> Какой способ запуска лучше для сдачи работы: `python main.py` или `uvicorn main:app --reload`? Мне важно получить максимальный балл.

**Ответ ИИ:**
ИИ подробно сравнил два способа. Объяснил, что `uvicorn main:app --reload` — это профессиональный стандарт, так как:
- Автоматически перезагружает сервер при изменениях кода
- Легче интегрировать тесты
- Лучше соответствует industry best practices

Способ с `python main.py` назвал "любительским". Рекомендовал использовать второй способ для максимального балла.

**Мои действия:**
Я приняла рекомендацию и теперь всегда запускаю сервер командой `uvicorn main:app --reload --host 0.0.0.0 --port 8000`. Также добавила эту команду в README.md.

---

### Промпт 4 (добавление валидации total_price)
**Инструмент:** DeepSeek  
**Промпт:** 
> Добавь в модель Order поле total_price с валидацией. Цена не может быть отрицательной. Обнови Pydantic модели и эндпоинты, если нужно.

**Ответ ИИ:**
ИИ предложил добавить в `OrderCreate` и `OrderUpdate`:
```python
total_price: float = Field(default=0.0, ge=0, description="Итоговая цена")
```
Объяснил, что `ge=0` означает "greater than or equal to 0" — цена не может быть отрицательной. Pydantic автоматически вернёт 422 при попытке передать отрицательное значение.

**Мои действия:**
Я добавила это поле в обе модели. Также обновила тесты: добавила `test_create_order_negative_price` и `test_update_order_negative_price`, которые проверяют, что API возвращает 422 при отрицательной цене.

---

### Итого по заданию 1
- **Количество промптов:** 4
- **Что пришлось исправлять вручную:** удаление блока `if __name__ == "__main__"`
- **Время:** ~35 минут
- **Результат:** Рабочий CRUD API, доступный по адресу `http://localhost:8000/docs`

---

## Задание 2: Генерация тестов

### Промпт 1 (базовые тесты)
**Инструмент:** DeepSeek  
**Промпт:** 
> Напиши pytest тесты для предыдущего FastAPI CRUD приложения (заказы автосервиса). Покрой: создание заказа (POST /orders), получение списка (GET /orders), получение одного заказа (GET /orders/{id}), обновление (PUT /orders/{id}), удаление (DELETE /orders/{id}), а также ошибки: невалидные данные и заказ не найден. Используй TestClient из fastapi.testclient.

**Ответ ИИ:**
ИИ выдал полный файл `test_main.py` с 12 тестами. Включил:
- Фикстуру `sample_order` для создания тестового заказа
- Фикстуру `cleanup` для очистки после тестов
- Тесты для каждого эндпоинта
- Тесты для ошибок 404 (заказ не найден) и 422 (невалидные данные)

**Мои действия:**
Я создала файл `test_main.py`, скопировала код. Установила `pytest` и `httpx`. Запустила тесты командой `pytest test_main.py -v`.

**Результат:** Все 12 тестов прошли успешно.

---

### Промпт 2 (граничные случаи)
**Инструмент:** DeepSeek  
**Промпт:** 
> Добавь тесты на граничные случаи для автосервиса: очень длинное описание (1000 символов), отрицательная цена, недопустимый статус, пустые обязательные поля.

**Ответ ИИ:**
ИИ добавил 6 новых тестов:
- `test_create_order_with_long_description` — проверка, что описание длиной 1000 символов принимается
- `test_create_order_negative_price` — проверка, что отрицательная цена возвращает 422
- `test_create_order_invalid_status` — проверка, что недопустимый статус возвращает 422
- `test_create_order_missing_required_fields` — проверка, что отсутствие owner_name или description возвращает 422
- `test_partial_update_order_invalid_status` — проверка недопустимого статуса при PATCH
- `test_update_order_negative_price` — проверка отрицательной цены при PUT

**Мои действия:**
Я добавила эти тесты в `test_main.py`. Обновила фикстуру `sample_order`, чтобы она создавала заказ с корректными данными.

**Результат:** Все 18 тестов прошли успешно. Покрытие составило 99%.

---

### Промпт 3 (отчёт о покрытии)
**Инструмент:** DeepSeek  
**Промпт:** 
> Как запустить pytest с отчетом о покрытии кода? Я хочу увидеть, какие строки не покрыты тестами.

**Ответ ИИ:**
ИИ объяснил, что нужно установить `pytest-cov`:
```bash
pip install pytest-cov
```
Затем запускать:
```bash
pytest test_main.py -v --cov=main --cov-report=term-missing
```
Объяснил, что `--cov=main` означает "измерять покрытие для модуля main", а `--cov-report=term-missing` покажет непокрытые строки прямо в терминале.

**Мои действия:**
Я установила `pytest-cov` и запустила тесты с отчётом.

**Результат:**
```
Name      Stmts   Miss  Cover   Missing
---------------------------------------
main.py      79      1    99%   35
```
Непокрытой оказалась строка 35 — блок с `OrderResponse` (который используется только как модель, не требует отдельного тестирования). Преподаватель засчитал 99% как отличный результат.

---

### Промпт 4 (объяснение coverage)
**Инструмент:** DeepSeek  
**Промпт:** 
> Покрытие 99% — это хорошо? Требование было 70%. Стоит ли пытаться достичь 100%?

**Ответ ИИ:**
ИИ объяснил, что 99% — это отличный результат. 100% покрытие часто не нужно и даже вредно, потому что заставляет писать тесты для тривиального кода (например, строки с `pass`, `return None`, декораторы). Сказал, что преподаватель оценит высокое покрытие, но не будет снижать оценку за 99% вместо 100%.

**Мои действия:**
Я решила оставить 99% и не тратить время на погоню за 100%. В отчёт добавила скриншот с результатом тестов.

---
### Промпт 5 (обновление тестов для работы с Docker и SQLite)
**Инструмент:** DeepSeek  
**Промпт:** 
> Тесты падают с ошибкой подключения к БД, потому что запускаются вне Docker. Как переделать тесты, чтобы они работали и локально, и в Docker? Используй SQLite in-memory для тестов.

**Ответ ИИ:**
ИИ предложил:
1. Создать тестовую БД SQLite в памяти
2. Переопределить зависимость `get_db` через `app.dependency_overrides`
3. Убрать фикстуру `cleanup`, использовать прямую очистку БД

**Мои действия:**
Я полностью переписала `test_main.py`:
- Добавила `SQLALCHEMY_DATABASE_URL = "sqlite:///./test.db"`
- Создала `TestingSessionLocal` и `override_get_db()`
- Подменила зависимость в приложении: `app.dependency_overrides[get_db] = override_get_db`
- Обновила тесты для работы с новой БД

**Результат:** Тесты проходят и локально, и в Docker (18 passed, покрытие 94%)

---

### Итого по заданию 2
- **Количество промптов:** 5
- **Что пришлось исправлять вручную:** ничего — тесты работали с первого раза
- **Время:** ~20 минут
- **Результат:** 18 тестов, покрытие 94% (локально и в Docker)

---

## Задание 3: Рефакторинг «плохого» кода

### Промпт 1 (создание плохого кода)
**Инструмент:** DeepSeek  
**Промпт:** 
> Напиши плохо написанную функцию для расчёта стоимости ремонта в автосервисе. Проблемы: магические числа, неинформативные имена переменных, длина >30 строк, отсутствие обработки ошибок, дублирование кода. Функция должна принимать часы работы, стоимость запчастей, сложность, срочность, скидку, статус постоянного клиента.

**Ответ ИИ:**
ИИ выдал функцию `calc(a, b, c, d, e, f)` следующего содержания:
```python
def calc(a, b, c, d, e, f):
    # a - часы работы
    # b - стоимость запчастей
    # c - сложность (1-3)
    # d - срочность (0/1)
    # e - скидка в процентах
    # f - постоянный клиент (0/1)
    
    x = 0
    if c == 1:
        x = 1200
    elif c == 2:
        x = 1800
    elif c == 3:
        x = 2500
    else:
        x = 1500
    
    if d == 1:
        w = a * x * 1.5
    else:
        w = a * x
    
    w = w + b
    
    if e > 0:
        if e > 30:
            e = 30
        w = w - w * e / 100
    
    if f == 1:
        w = w * 0.95
    
    if w < 0:
        w = 0
    
    if w > 50000:
        w = w * 0.9
    
    t = w
    for i in range(3):
        t = t + 500
    
    return t
```
Функция содержит все указанные проблемы: магические числа (1200, 1800, 2500, 1.5, 30, 0.95, 50000, 0.9, 500, 3), неинформативные имена, длина 35 строк, нет обработки ошибок.

**Мои действия:**
Я сохранила этот код в файл `bad_code.py` и добавила демонстрационный блок `if __name__ == "__main__"` для проверки работы.

---

### Промпт 2 (рефакторинг)
**Инструмент:** DeepSeek  
**Промпт:** 
> Выполни рефакторинг этой функции. Вынеси магические числа в константы, добавь type hints, docstring, обработку ошибок (отрицательные часы, отрицательная стоимость запчастей, скидка >100%). Разбей на несколько маленьких функций. Объясни каждое изменение.

**Ответ ИИ:**
ИИ выдал полностью переработанный код в файле `good_code.py`:

1. **Создал классы для констант:**
```python
class Complexity(Enum):
    LOW = 1
    MEDIUM = 2
    HIGH = 3

class HourlyRate:
    LOW = 1200
    MEDIUM = 1800
    HIGH = 2500
    DEFAULT = 1500

class DiscountLimits:
    MAX_PERCENT = 30
    LOYALTY_DISCOUNT = 0.95
    HIGH_COST_THRESHOLD = 50000
    HIGH_COST_DISCOUNT = 0.9

class Constants:
    URGENT_MULTIPLIER = 1.5
    EXTRA_FEE = 500
    EXTRA_FEE_ITERATIONS = 3
```

2. **Добавил type hints и docstring:**
```python
def calculate_repair_cost(
    hours: Union[int, float],
    parts_cost: Union[int, float],
    complexity: int,
    is_urgent: bool,
    discount_percent: Union[int, float],
    is_loyal_customer: bool
) -> float:
```

3. **Добавил обработку ошибок:**
```python
if hours < 0:
    raise ValueError(f"Часы не могут быть отрицательными: {hours}")
if parts_cost < 0:
    raise ValueError(f"Стоимость запчастей не может быть отрицательной: {parts_cost}")
if discount_percent < 0 or discount_percent > 100:
    raise ValueError(f"Скидка должна быть от 0 до 100: {discount_percent}")
```

4. **Вынес логику в отдельные функции:**
- `_get_hourly_rate(complexity)` — определение часовой ставки
- `_apply_discount(amount, discount_percent)` — применение скидки

5. **Добавил подробное объяснение каждого изменения** в виде таблицы.

**Мои действия:**
Я сохранила отрефакторенный код в `good_code.py`. Также создала таблицу с объяснением изменений для `PROMPT_LOG.md`.

---

### Промпт 3 (уточнение про type hints)
**Инструмент:** DeepSeek  
**Промпт:** 
> В чём разница между `Union[int, float]` и просто `float` в type hints? Можно ли упростить?

**Ответ ИИ:**
ИИ объяснил, что в Python `float` принимает и целые числа (int) неявно. Например, `5` автоматически преобразуется в `5.0` при передаче в функцию, ожидающую `float`. Поэтому `Union[int, float]` избыточен, можно писать просто `float`. Однако если важна строгая типизация, можно оставить `Union`. Для учебного проекта достаточно `float`.

**Мои действия:**
Я упростила type hints в `good_code.py`, заменив `Union[int, float]` на `float`. Код стал чище.

---

### Итого по заданию 3
- **Количество промптов:** 3
- **Что пришлось исправлять вручную:** оформление пояснений, упрощение type hints
- **Время:** ~15 минут
- **Результат:** `bad_code.py` (плохая функция) и `good_code.py` (отрефакторенная)

---

## Задание 4: Docker + Alembic

### Промпт 1 (базовые Dockerfile и docker-compose)
**Инструмент:** DeepSeek  
**Промпт:** 
> Создай Dockerfile и docker-compose.yml для FastAPI приложения автосервиса. Используй PostgreSQL, добавь переменные окружения.

**Ответ ИИ:**
ИИ выдал:
- `Dockerfile` с образом `python:3.11-slim`, копированием `requirements.txt`, установкой зависимостей
- `docker-compose.yml` с двумя сервисами: `db` (postgres:15-alpine) и `app` (сборка из Dockerfile)
- `requirements.txt` с `fastapi`, `uvicorn`, `pydantic`, `sqlalchemy`, `psycopg2-binary`, `python-dotenv`
- `.env.example` с переменными `DATABASE_URL`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`

**Мои действия:**
Я создала все указанные файлы. Запустила `docker-compose up --build`. Контейнеры поднялись, но Alembic не был настроен. Пришлось делать дополнительные запросы.

---

### Промпт 2 (добавление Alembic)
**Инструмент:** DeepSeek  
**Промпт:** 
> Добавь Alembic для миграций. Обнови docker-compose.yml, чтобы миграции применялись автоматически при запуске контейнера. Добавь `database.py` и `models.py`.

**Ответ ИИ:**
ИИ выдал:
- `database.py` — настройка SQLAlchemy, создание `engine`, `SessionLocal`, `get_db`
- `models.py` — модель `Order` с колонками, `OrderStatus` (Enum)
- Обновлённый `docker-compose.yml` с командой:
```yaml
command: >
  sh -c "alembic upgrade head &&
         uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
```

**Мои действия:**
Я создала `database.py` и `models.py`. Обновила `main.py`, чтобы использовать БД вместо списка в памяти.

---

### Промпт 3 (инициализация Alembic)
**Инструмент:** DeepSeek  
**Промпт:** 
> `alembic init alembic` выдает ошибку "Directory already exists". Что делать? Я внутри контейнера `docker exec -it autoservice_app /bin/bash`.

**Ответ ИИ:**
ИИ объяснил, что папка `alembic` уже существует, но она может быть неполной (отсутствует `script.py.mako`). Предложил:
1. Удалить папку: `rm -rf alembic/`
2. Выполнить `alembic init alembic` заново
3. Настроить `alembic.ini` (добавить секцию `[alembic]` и `sqlalchemy.url`)

**Мои действия:**
Я удалила папку, создала заново, настроила `alembic.ini`.

---

### Промпт 4 (ошибка в alembic.ini)
**Инструмент:** DeepSeek  
**Промпт:** 
> Ошибка: "MissingSectionHeaderError: File contains no section headers" в alembic.ini. Текст ошибки показывает, что файл начинается с `sqlalchemy.url = ...` без секции.

**Ответ ИИ:**
ИИ объяснил, что `alembic.ini` должен начинаться с секции `[alembic]`. Каждый параметр должен быть внутри секции. Исправленный формат:
```ini
[alembic]
script_location = alembic
sqlalchemy.url = postgresql://postgres:postgres123@db:5432/autoservice_db
```

**Мои действия:**
Я исправила `alembic.ini`, добавив секцию `[alembic]` в начало файла.

---

### Промпт 5 (ошибка с black)
**Инструмент:** DeepSeek  
**Промпт:** 
> Ошибка: "Could not find entrypoint console_scripts.black" при создании миграции. Это критично?

**Ответ ИИ:**
ИИ объяснил, что это не критичная ошибка. `black` — это форматтер кода, который Alembic пытается запустить после генерации миграции. Если `black` не установлен, Alembic выдаёт предупреждение, но миграция создаётся. Чтобы убрать ошибку, можно удалить секцию `[post_write_hooks]` из `alembic.ini`.

**Мои действия:**
Я удалила секцию `post_write_hooks` из `alembic.ini`. Ошибка исчезла.

---

### Промпт 6 (проверка миграций без psql)
**Инструмент:** DeepSeek  
**Промпт:** 
> Как проверить, что миграция применилась? В контейнере app нет psql. Команда `psql: command not found`.

**Ответ ИИ:**
ИИ предложил два способа:
1. Войти в контейнер db: `docker exec -it autoservice_db psql -U postgres -d autoservice_db`
2. Использовать Python внутри контейнера app:
```bash
python -c "import psycopg2; conn = psycopg2.connect(host='db', user='postgres', password='postgres123', dbname='autoservice_db'); cur = conn.cursor(); cur.execute('SELECT table_name FROM information_schema.tables WHERE table_schema='public''); print([row[0] for row in cur.fetchall()])"
```

**Мои действия:**
Я использовала первый способ. Подключилась к контейнеру db и выполнила `\dt`. Увидела таблицы `alembic_version` и `orders`. Миграция применилась успешно.

---

### Промпт 7 (удаление create_all)
**Инструмент:** DeepSeek  
**Промпт:** 
> В `main.py` есть строка `models.Base.metadata.create_all(bind=engine)`. Она дублирует Alembic. Нужно ли её убрать? Если убрать, не сломается ли приложение?

**Ответ ИИ:**
ИИ объяснил, что:
- Эта строка создаёт таблицы автоматически, даже если Alembic не запускался
- Это полезно для быстрого прототипирования, но в production это плохая практика
- При правильной настройке Alembic эта строка не нужна
- Если её убрать, при первом запуске (без миграций) таблицы не создадутся, и приложение упадёт
- Поэтому нужно убедиться, что `alembic upgrade head` выполняется до запуска приложения

**Мои действия:**
Я удалила эту строку из `main.py`, потому что в `docker-compose.yml` уже была команда `alembic upgrade head`. Приложение работает, таблицы создаются через миграции.

---

### Итого по заданию 4
- **Количество промптов:** 7
- **Что пришлось исправлять вручную:** настройка `alembic.ini`, удаление `post_write_hooks`, удаление `create_all()`
- **Время:** ~80 минут
- **Результат:** Полностью настроенный Docker + PostgreSQL + Alembic

---

## Задание 5: Объяснение сложного кода

### Промпт 1 (общее объяснение)
**Инструмент:** DeepSeek  
**Промпт:** 
> Объясни, как работает функция `calculate_repair_cost` из `good_code.py`. Разбери каждый шаг: валидация, расчёт ставки, стоимости работ, скидки, надбавки.

**Ответ ИИ:**
ИИ выдал подробное объяснение:
1. **Валидация:** проверка, что часы, цена запчастей и скидка находятся в допустимых пределах. Если нет — `ValueError`.
2. **Часовая ставка:** определяется сложностью через `_get_hourly_rate()`. Возвращает 1200, 1800, 2500 или 1500 (по умолчанию).
3. **Стоимость работ:** `часы * ставка`. Если срочно — умножается на 1.5.
4. **Добавление запчастей:** `labor_cost + parts_cost`.
5. **Скидка:** применяется через `_apply_discount()`, ограничена 30%.
6. **Скидка постоянному клиенту:** умножается на 0.95 (5% скидка).
7. **Скидка при высокой стоимости:** если сумма > 50000, умножается на 0.9 (10% скидка).
8. **Дополнительные сборы:** добавляется 1500 рублей (500 × 3).
9. **Округление и защита:** `round(max(total, 0))` — гарантирует неотрицательность и округление до целых.

---

### Промпт 2 (магические числа)
**Инструмент:** DeepSeek  
**Промпт:** 
> Объясни, что означают числа 1.5, 0.95, 50000, 0.9, 500, 3, 30. Почему именно такие значения?

**Ответ ИИ:**
ИИ создал таблицу:

| Число | Значение | Почему именно такое |
|-------|----------|---------------------|
| 1.5 | Коэффициент срочности | Надбавка 50% за срочный ремонт |
| 0.95 | Скидка постоянному клиенту | 5% скидка для лояльных клиентов |
| 50000 | Порог для крупного ремонта | Ремонт дороже 50 000 руб. получает доп. скидку |
| 0.9 | Скидка при крупном ремонте | 10% скидка при сумме >50 000 руб. |
| 500 | Базовая дополнительная плата | Диагностика, мойка, утилизация |
| 3 | Количество дополнительных услуг | Диагностика, мойка, утилизация |
| 30 | Максимальная скидка в % | Сервис не даёт скидку больше 30% |

---

### Промпт 3 (round и max)
**Инструмент:** DeepSeek  
**Промпт:** 
> Зачем используется `round(max(total, 0))`? Не проще ли просто вернуть total?

**Ответ ИИ:**
ИИ объяснил:
- `max(total, 0)` — защита от гипотетического сценария, когда после всех вычислений сумма стала отрицательной (например, если скидка >100%). Без этой защиты функция могла бы вернуть отрицательное число, что экономически бессмысленно.
- `round()` — стоимость ремонта должна быть целым числом рублей (в реальных сервисах не используют копейки).

---

### Промпт 4 (потенциальные проблемы)
**Инструмент:** DeepSeek  
**Промпт:** 
> Какие потенциальные проблемы есть в этой логике расчёта? Что можно улучшить?

**Ответ ИИ:**
ИИ перечислил:
1. **Магические числа** — нужно вынести в конфиг
2. **Жёсткая логика скидок** — сделать настраиваемой через БД
3. **Порог 50000 проверяется ДО добавления сборов** — можно проверять после
4. **Последовательность скидок влияет на результат** — задокументировать порядок
5. **Нет логирования** — добавить для отладки

---

### Итого по заданию 5
- **Количество промптов:** 4
- **Что пришлось исправлять вручную:** ничего
- **Время:** ~30 минут
- **Результат:** Подробное объяснение с таблицами и примерами

---

## Задание 6: Генерация README.md

### Промпт 1 (базовый README)
**Инструмент:** DeepSeek  
**Промпт:** 
> Создай README.md для проекта "Автосервис: управление заказами". Включи название, описание, технологический стек, инструкцию по установке и запуску (3 способа), описание API эндпоинтов.

**Ответ ИИ:**
ИИ выдал базовый README с заголовками, списком технологий, инструкциями.

**Мои действия:**
Я скопировала содержимое в `README.md`.

---

### Промпт 2 (бейджи)
**Инструмент:** DeepSeek  
**Промпт:** 
> Добавь бейджи (badges) в начало README: для Python, FastAPI, PostgreSQL, Docker, лицензии.

**Ответ ИИ:**
ИИ предоставил код бейджей с shields.io:
```markdown
[![Python](https://img.shields.io/badge/Python-3.11-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104-green.svg)](https://fastapi.tiangolo.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue.svg)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-24.0-blue.svg)](https://www.docker.com/)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
```

**Мои действия:**
Я добавила бейджи в начало файла.

---

### Промпт 3 (таблица переменных)
**Инструмент:** DeepSeek  
**Промпт:** 
> Как оформить таблицу переменных окружения в Markdown? У меня есть переменные DATABASE_URL, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, APP_ENV, APP_PORT.

**Ответ ИИ:**
ИИ показал правильный синтаксис:
```markdown
| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `DATABASE_URL` | URL подключения к PostgreSQL | `postgresql://postgres:postgres123@db:5432/autoservice_db` |
```

**Мои действия:**
Я переделала таблицу в правильный формат.

---

### Промпт 4 (результаты тестов)
**Инструмент:** DeepSeek  
**Промпт:** 
> Добавь в README секцию с результатами тестов: 18 passed, покрытие 99%.

**Ответ ИИ:**
ИИ предложил добавить:
```markdown
## 🧪 Запуск тестов

pytest test_main.py -v --cov=main --cov-report=term-missing

**Результат тестов:** 18 passed, покрытие 99%

**Мои действия:**
Я добавила эту секцию в README.

---

### Итого по заданию 6
- **Количество промптов:** 4
- **Что пришлось исправлять вручную:** форматирование таблиц, код-блоков
- **Время:** ~35 минут
- **Результат:** Полный README.md с бейджами, таблицами, инструкциями

---

## Задание 7: Генерация миграций БД

### Промпт 1 (добавление таблицы masters)
**Инструмент:** DeepSeek  
**Промпт:** 
> У меня уже есть таблица orders. Добавь таблицу masters (мастера) и свяжи её с заказами. Связь: один мастер может иметь много заказов (one-to-many). Добавь индексы на specialization, status, master_id.

**Ответ ИИ:**
ИИ выдал Alembic миграцию:
```python
def upgrade():
    op.create_table('masters',
        sa.Column('id', sa.Integer(), nullable=False),
        sa.Column('name', sa.String(100), nullable=False),
        sa.Column('specialization', sa.String(50), nullable=False),
        sa.Column('phone', sa.String(20)),
        sa.Column('hire_date', sa.DateTime(), server_default=sa.text('now()')),
        sa.PrimaryKeyConstraint('id')
    )
    op.create_index('ix_masters_specialization', 'masters', ['specialization'])
    op.add_column('orders', sa.Column('master_id', sa.Integer(), nullable=True))
    op.create_foreign_key('fk_orders_master_id', 'orders', 'masters', ['master_id'], ['id'], ondelete='SET NULL')
    op.create_index('ix_orders_master_id', 'orders', ['master_id'])
    op.create_index('ix_orders_status', 'orders', ['status'])
```

**Мои действия:**
Я создала файл миграции `alembic/versions/2a3b4c5d6e7f_add_masters_table.py` и применила `alembic upgrade head`.

---

### Промпт 2 (raw SQL)
**Инструмент:** DeepSeek  
**Промпт:** 
> Сгенерируй raw SQL для этой миграции (CREATE TABLE, ALTER TABLE, CREATE INDEX). Нужно для отчёта.

**Ответ ИИ:**
ИИ выдал SQL скрипт:
```sql
CREATE TABLE masters (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    specialization VARCHAR(50) NOT NULL,
    phone VARCHAR(20),
    hire_date TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_masters_specialization ON masters(specialization);

ALTER TABLE orders ADD COLUMN master_id INTEGER;
ALTER TABLE orders ADD CONSTRAINT fk_orders_master_id FOREIGN KEY (master_id) REFERENCES masters(id) ON DELETE SET NULL;

CREATE INDEX idx_orders_master_id ON orders(master_id);
CREATE INDEX idx_orders_status ON orders(status);
```

**Мои действия:**
Я сохранила этот скрипт в файл `migrations/add_masters_table.sql`.

---

### Промпт 3 (ошибка с ENUM)
**Инструмент:** DeepSeek  
**Промпт:** 
> При проверке SQL-запроса ошибка: "invalid input value for enum orderstatus: 'выдан'". Почему? У меня в модели определён OrderStatus с русскими значениями.

**Ответ ИИ:**
ИИ попросил показать вывод `SELECT enum_range(NULL::orderstatus);`. Я выполнила и увидела: `{ACCEPTED,IN_PROGRESS,READY,COMPLETED}`. ИИ объяснил, что в БД ENUM хранит английские значения, а не русские. Нужно использовать `'COMPLETED'` вместо `'выдан'`.

**Мои действия:**
Я исправила SQL-запрос, заменив `'выдан'` на `'COMPLETED'`. Ошибка исчезла.

---

### Промпт 4 (обновление models.py)
**Инструмент:** DeepSeek  
**Промпт:** 
> Обнови models.py, чтобы включить модель Master и связь с Order.

**Ответ ИИ:**
ИИ выдал обновлённый `models.py` с классом `Master` и добавлением поля `master_id` и `relationship` в `Order`.

**Мои действия:**
Я заменила содержимое `models.py` на новую версию.

---

### Итого по заданию 7
- **Количество промптов:** 4
- **Что пришлось исправлять вручную:** замена `'выдан'` на `'COMPLETED'` в SQL
- **Время:** ~40 минут
- **Результат:** Таблица masters, связь с orders, индексы

---

## Задание 8: Поиск уязвимостей

### Промпт 1 (анализ уязвимостей)
**Инструмент:** DeepSeek  
**Промпт:** 
> Ты — senior security engineer. Найди уязвимости в моём FastAPI коде. Ищи: SQL-инъекции, XSS, отсутствие аутентификации, проблемы с CORS, утечку данных, rate limiting, path traversal. Выдай отчёт в таблице с критичностью и рекомендациями.

**Ответ ИИ:**
ИИ выдал таблицу с 7 уязвимостями:

| № | Уязвимость | Критичность | Описание | Рекомендация |
|---|------------|-------------|----------|--------------|
| 1 | Отсутствие аутентификации | 🔴 ВЫСОКИЙ | Любой может создавать, читать, обновлять заказы | Добавить JWT |
| 2 | Отсутствие CORS | 🟡 СРЕДНИЙ | API не настроен для работы с другими доменами | Добавить CORSMiddleware |
| 3 | XSS через description | 🟡 СРЕДНИЙ | description может содержать JavaScript | Экранировать HTML |
| 4 | Утечка данных | 🟡 СРЕДНИЙ | Возвращается вся ORM-модель | Использовать Pydantic схемы |
| 5 | Отсутствие rate limiting | 🟡 СРЕДНИЙ | Нет защиты от DoS | Добавить slowapi |
| 6 | Отсутствие health check | 🟢 НИЗКИЙ | Нет эндпоинта для проверки работы | Добавить GET /health |
| 7 | Path traversal | 🟢 НИЗКИЙ | Неактуально (нет загрузки файлов) | При добавлении файлов использовать secure_filename |

---

### Промпт 2 (CORS)
**Инструмент:** DeepSeek  
**Промпт:** 
> Как добавить CORS в FastAPI? Дай готовый код.

**Ответ ИИ:**
ИИ предоставил код:
```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "http://localhost:8080"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**Мои действия:**
Я добавила этот код в `main.py` после создания `app`.

---

### Промпт 3 (XSS защита)
**Инструмент:** DeepSeek  
**Промпт:** 
> Как защититься от XSS в FastAPI? Нужно экранировать HTML в поле description.

**Ответ ИИ:**
ИИ предложил использовать `html.escape()`:
```python
import html
description=html.escape(order.description)
```

**Мои действия:**
Я добавила `import html` и обернула `description` в `html.escape()` в `create_order`, `update_order` и `partial_update_order`.

---

### Промпт 4 (проверка CORS)
**Инструмент:** DeepSeek  
**Промпт:** 
> Как проверить, что CORS работает? `curl` не показывает заголовки `access-control-allow-origin`.

**Ответ ИИ:**
ИИ объяснил, что CORS заголовки отправляются только при кросс-доменных запросах (когда Origin отличается от текущего домена). Для проверки нужно отправить OPTIONS запрос:
```bash
curl -X OPTIONS http://localhost:8000/orders -H "Origin: http://localhost:3000" -H "Access-Control-Request-Method: GET" -v
```
Или проверить через браузер (F12 → Network → запрос → Response Headers).

**Мои действия:**
Я проверила через браузер — заголовки присутствуют.

---

### Промпт 5 (health check)
**Инструмент:** DeepSeek  
**Промпт:** 
> Добавь health check эндпоинт. Он должен возвращать статус и текущее время.

**Ответ ИИ:**
ИИ выдал код:
```python
@app.get("/health", tags=["Health"])
async def health_check():
    return {"status": "ok", "timestamp": datetime.now().isoformat()}
```

**Мои действия:**
Я добавила этот эндпоинт в `main.py`.

---

### Итого по заданию 8
- **Количество промптов:** 5
- **Что пришлось исправлять вручную:** внедрение всех исправлений в код
- **Время:** ~30 минут
- **Результат:** Исправлены CORS, XSS, утечка данных, добавлен health check

---

## Задание 9: Генерация SQL-запроса

### Промпт 1 (запрос топ-3 мастеров)
**Инструмент:** DeepSeek  
**Промпт:** 
> Напиши SQL-запрос для отчёта: "Топ-3 мастера по количеству выполненных заказов за последние 30 дней с указанием специализации и общей выручки". Есть таблицы masters и orders.

**Ответ ИИ:**
ИИ выдал запрос:
```sql
SELECT 
    m.name AS master_name,
    m.specialization,
    COUNT(o.id) AS orders_completed,
    COALESCE(SUM(o.total_price), 0) AS total_revenue
FROM masters m
INNER JOIN orders o ON m.id = o.master_id
WHERE o.status = 'выдан'
    AND o.created_at >= NOW() - INTERVAL '30 days'
GROUP BY m.id, m.name, m.specialization
ORDER BY orders_completed DESC
LIMIT 3;
```

---

### Промпт 2 (объяснение)
**Инструмент:** DeepSeek  
**Промпт:** 
> Объясни логику этого запроса по шагам.

**Ответ ИИ:**
ИИ объяснил:
1. `FROM masters m` — начинаем с таблицы мастеров
2. `INNER JOIN orders o ON m.id = o.master_id` — соединяем с заказами
3. `WHERE o.status = 'выдан'` — только выполненные заказы
4. `AND o.created_at >= NOW() - INTERVAL '30 days'` — за последние 30 дней
5. `GROUP BY m.id, m.name, m.specialization` — группируем по мастерам
6. `COUNT(o.id)` — считаем количество заказов
7. `SUM(o.total_price)` — суммируем выручку
8. `ORDER BY orders_completed DESC` — сортируем по убыванию
9. `LIMIT 3` — берём топ-3

---

### Промпт 3 (COALESCE)
**Инструмент:** DeepSeek  
**Промпт:** 
> Что делает COALESCE в этом запросе? Зачем он нужен?

**Ответ ИИ:**
ИИ объяснил, что `COALESCE(SUM(o.total_price), 0)` возвращает 0, если `SUM` вернул NULL (например, если у мастера нет заказов). Без `COALESCE` в результате был бы NULL, что может вызвать ошибки в приложении.

---

### Промпт 4 (проверка запроса)
**Инструмент:** DeepSeek  
**Промпт:** 
> Как проверить запрос без psql в контейнере app? У меня нет psql.

**Ответ ИИ:**
ИИ предложил использовать Python:
```bash
docker exec -it autoservice_app python -c "
import psycopg2
conn = psycopg2.connect(host='db', user='postgres', password='postgres123', dbname='autoservice_db')
cur = conn.cursor()
cur.execute('''SELECT m.name, COUNT(o.id) FROM masters m LEFT JOIN orders o ON m.id = o.master_id GROUP BY m.id''')
print(cur.fetchall())
"
```

**Мои действия:**
Я использовала этот способ и убедилась, что запрос выполняется без ошибок.

---

### Итого по заданию 9
- **Количество промптов:** 4
- **Что пришлось исправлять вручную:** замена `'выдан'` на `'COMPLETED'` из-за ENUM
- **Время:** ~20 минут
- **Результат:** SQL-запрос, объяснение, проверка

---

## Задание 10: Генерация регулярного выражения

### Промпт 1 (регулярное выражение)
**Инструмент:** DeepSeek  
**Промпт:** 
> Сгенерируй регулярное выражение для валидации российского госномера автомобиля в системе автосервиса. Формат: буква (А,В,Е,К,М,Н,О,Р,С,Т,У,Х) + 3 цифры + 2 буквы + код региона (2-3 цифры).

**Ответ ИИ:**
ИИ выдал regex:
```python
RUSSIAN_PLATE_PATTERN = r'^[АВЕКМНОРСТУХ]\d{3}[АВЕКМНОРСТУХ]{2}\d{2,3}$'
```
С пояснением:
- `^` — начало строки
- `[АВЕКМНОРСТУХ]` — одна из разрешённых букв
- `\d{3}` — три цифры
- `[АВЕКМНОРСТУХ]{2}` — две разрешённые буквы
- `\d{2,3}` — две или три цифры (регион)
- `$` — конец строки

---

### Промпт 2 (скрипт для тестирования)
**Инструмент:** DeepSeek  
**Промпт:** 
> Напиши Python скрипт для тестирования этого регулярного выражения на наборе из 5 валидных и 5 невалидных примеров. Скрипт должен выводить: пример → "✓ Валидный" или "✗ Невалидный".

**Ответ ИИ:**
ИИ выдал полный скрипт `validate_plate.py` с:
- Функцией `validate_plate(plate: str) -> bool`
- Списком тестовых случаев (5 валидных, 5 невалидных)
- Циклом, который выводит результат для каждого примера

---

### Промпт 3 (добавление ожидаемых результатов)
**Инструмент:** DeepSeek  
**Промпт:** 
> Добавь в скрипт вывод "Ожидалось: валидный/невалидный" для удобства отладки. Сравни фактический результат с ожидаемым.

**Ответ ИИ:**
ИИ обновил скрипт, добавив в тестовые данные кортежи `(plate, expected)` и сравнивая результат с ожиданием. Теперь при несовпадении выводится `[FAIL]`.

---

### Промпт 4 (запуск скрипта)
**Инструмент:** DeepSeek  
**Промпт:** 
> Как запустить скрипт и что я должна увидеть?

**Ответ ИИ:**
ИИ объяснил:
```bash
python validate_plate.py
```
Ожидаемый вывод:
```
============================================================
Тестирование регулярного выражения для госномера авто
============================================================
[OK] А123ВС77        → ✓ Валидный
[OK] В777АА199       → ✓ Валидный
[OK] а123вс77        → ✗ Невалидный
...
✅ Все тесты пройдены!
```

**Мои действия:**
Я запустила скрипт. Все 14 тестов прошли успешно.

---

### Итого по заданию 10
- **Количество промптов:** 4
- **Что пришлось исправлять вручную:** ничего
- **Время:** ~20 минут
- **Результат:** `validate_plate.py` с regex и тестами

---

## Общий итог по лабораторной работе №12

| Показатель | Значение |
|------------|----------|
| **Всего промптов** | 43 |
| **Всего заданий** | 10 |
| **Общее время** | ~5 часов |
| **Основные сложности** | Alembic настройка (7 промптов), ENUM значения, CORS проверка, форматирование README |
| **Что сделано вручную** | настройка alembic.ini и env.py, удаление create_all(), замена статусов в SQL, CORS middleware, XSS защита |

### Распределение промптов по заданиям

| Задание | Тема | Количество промптов |
|---------|------|---------------------|
| 1 | CRUD приложение | 4 |
| 2 | Тесты | 4 |
| 3 | Рефакторинг | 3 |
| 4 | Docker + Alembic | 7 |
| 5 | Объяснение кода | 4 |
| 6 | README.md | 4 |
| 7 | Миграции БД | 4 |
| 8 | Уязвимости | 5 |
| 9 | SQL запрос | 4 |
| 10 | Регулярное выражение | 4 |
| **Итого** | | **44** |
