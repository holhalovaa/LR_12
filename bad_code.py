"""
ПЛОХОЙ код для автосервиса (ДО рефакторинга)
Задача: расчёт стоимости ремонта
"""

def calc(a, b, c, d, e, f):
    # a - часы работы
    # b - стоимость запчастей
    # c - сложность (1-3)
    # d - срочность (0/1)
    # e - скидка в процентах
    # f - постоянный клиент (0/1)
    
    x = 0
    
    if c == 1:
        x = 1200
    elif c == 2:
        x = 1800
    elif c == 3:
        x = 2500
    else:
        x = 1500
    
    if d == 1:
        w = a * x * 1.5
    else:
        w = a * x
    
    w = w + b
    
    if e > 0:
        if e > 30:
            e = 30
        w = w - w * e / 100
    
    if f == 1:
        w = w * 0.95
    
    if w < 0:
        w = 0
    
    if w > 50000:
        w = w * 0.9
    
    t = w
    for i in range(3):
        t = t + 500
    
    return t


# Демонстрация работы (для отчёта)
if __name__ == "__main__":
    result = calc(5, 3000, 2, 1, 10, 1)
    print(f"Результат (плохой код): {result}")